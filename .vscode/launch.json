{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Server",
            "program": "${workspaceFolder}/target/debug/barium-server",
            "args": ["server/config.json"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Server Debug"
        },
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Client",
            "program": "${workspaceFolder}/target/debug/barium-client",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Client Debug"
        }
    ]
}
